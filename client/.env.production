# Production: URL API công khai (Next embed lúc build — không đặt secret ở đây).
NEXT_PUBLIC_API_BASE_URL=https://api.example.com

# Production: mặc định vẫn bật chặn client qua NODE_ENV=production; có thể ghi đè.
NEXT_PUBLIC_REQUIRE_ACCESS_TOKEN=true
