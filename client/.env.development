# Dev: gọi API .NET khi chạy npm run dev (đổi port nếu khác launchSettings.json).
NEXT_PUBLIC_API_BASE_URL=https://localhost:7251

# true = chặn gọi API cần đăng nhập khi chưa có JWT (localStorage). Dev thường để false.
NEXT_PUBLIC_REQUIRE_ACCESS_TOKEN=false
