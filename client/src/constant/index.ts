/**
 * Hằng số ứng dụng (routes, keys, config tĩnh).
 */

export const ROUTES = {
  home: '/',
  /** Tổng quan khu quản trị (layout Managers). */
  dashboard: '/dashboard',
  rbac: '/rbac',
  users: '/users',
} as const
